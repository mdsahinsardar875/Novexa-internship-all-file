# Rename this file to .env before running the server

# Local MongoDB:
# MONGO_URI=mongodb://127.0.0.1:27017/novexa_ecommerce

# MongoDB Atlas (replace <username>, <password>, <cluster>):
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/novexa_ecommerce

PORT=5000
